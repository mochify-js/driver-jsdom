{
  "name": "@mochify/driver-jsdom",
  "version": "1.0.0",
  "description": "jsdom driver for Mochify",
  "homepage": "https://github.com/mochify-js/driver-jsdom",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "echo \"No unit tests yet\"",
    "test:all": "npm run test && npm run test:integration",
    "test:integration": "mocha -R spec '**/*.integration.js'",
    "coverage": "c8 --reporter=text --reporter=lcov npm run test:all",
    "coverage:check": "c8 --reporter=text --reporter=lcov --check-coverage --branches 100 --functions 100 --lines 100 npm run test:all",
    "build": "tsc --project tsconfig.pack.json",
    "clean": "rimraf --glob '!(node_modules)/**/*.d.ts' '*.d.ts' coverage",
    "prepack": "npm run clean && npm run build",
    "postpack": "npm run clean",
    "preversion": "npm run lint && npm run prettier:check && tsc && npm run coverage:check",
    "version": "changes --commits --footer",
    "postversion": "git push --follow-tags && npm publish",
    "prettier:check": "prettier --check '**/*.{js,json,md}'",
    "prettier:write": "prettier --write '**/*.{js,json,md}'",
    "prepare": "husky"
  },
  "keywords": [
    "mocha",
    "jsdom",
    "headless",
    "testing"
  ],
  "author": "Maximilian Antoni <mail@maxantoni.de> (http://maxantoni.de)",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "mocha": {
    "reporter": "dot",
    "require": "test/hooks.js",
    "ignore": "node_modules/**"
  },
  "dependencies": {
    "jsdom": "^26.1.0"
  },
  "peerDependencies": {
    "@mochify/mochify": "^1.0.1"
  },
  "devDependencies": {
    "@sinonjs/referee-sinon": "^12.0.0",
    "@studio/changes": "^3.0.0",
    "@studio/eslint-config": "^8.0.1",
    "@studio/related-tests": "^0.2.0",
    "@studio/tsconfig": "^1.3.0",
    "@types/node": "^24.10.1",
    "c8": "^10.1.3",
    "eslint": "^9.39.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "mocha": "^11.7.4",
    "prettier": "^3.6.2",
    "rimraf": "^6.1.0",
    "typescript": "^5.9.3"
  },
  "files": [
    "**/*.js",
    "**/*.d.ts",
    "!**/*.test.js",
    "!test/**",
    "!.*"
  ]
}
